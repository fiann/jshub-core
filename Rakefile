require 'rake'

# Task for RunCodeRun on 'gh-pages' branch
desc "Override the default task for RunCodeRun"
task :default do
  puts "This branch is not intended to be build-able and so does nothing."
end

